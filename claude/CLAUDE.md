@RTK.md

# Output style self-check

Before sending any reply longer than ~2 sentences, re-read output-styles/terse.md
and strip articles (a, an, the) that don't change meaning. Style setting reloads
every session — this note is for adherence, not wiring.
